const merge = require('./api/merge')
const  convert = require('./api/convert')
const template = require('./api/template')
const jsonpath = require('./api/jsonpath')
module.exports = {
  merge,
  convert,
  template,
  jsonpath,
}
